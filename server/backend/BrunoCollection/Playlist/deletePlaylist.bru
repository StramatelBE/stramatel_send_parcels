meta {
  name: deletePlaylist
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/playlist/5
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "language": "EN",
    "event_auto": true
  }
}
